K6BUS / PROTOBUZIG - TODO
Actualizado: 2026-09-09

PROPOSITO DE ESTE DOCUMENTO
Listado de lo que FALTA por hacer, en tres secciones:
  1. Pendientes por proyecto/funcionalidad (k6bus / protobuzig / compartido).
  2. Los mismos pendientes por tipo (leaks, double-free, fallos, robustez,
     nueva funcionalidad) con su prioridad.
  3. Resueltos (historial; los IDs citados alli pertenecen a la numeracion
     ANTERIOR, cerrada hoy 2026-09-09).

NUMERACION (reiniciada el 2026-09-09; generacion actual):
  D = double-free/UAF      F = fallo funcional     R = robustez/tests/deuda
  X = nueva funcionalidad / plan      L = leaks (vacia)
Los numeros arrancan en 1 por categoria y se asignan aqui y ahora; los IDs
antiguos (F1..F6, L1..L3, D2, R1..R8 de la generacion anterior) solo se
mencionan en la seccion 3 como historial. Prioridad: P1 alta, P2 media,
P3 baja/backlog.

===============================================================================
1. PENDIENTES POR PROYECTO / FUNCIONALIDAD
===============================================================================

PROTOBUZIG (generador, parser y andamiaje --ws)
-------------------------------------------------------------------------------
- L1 [P2] Fuga en la RUTA DE ERROR del parseo JSON generado: la rama
  .TF_JSON usa std.json.parseFromSliceLeaky (allocate = alloc_always), que NO
  libera las asignaciones parciales si el parseo falla tras reservar (p. ej.
  oneof con dos ramas -> UnexpectedToken). Con error temprano no fuga; con
  error tardio fuga parcial. Resolucion estudiada: usar std.json.parseFromSlice
  (arena, error-clean) y copiar el valor con un round-trip binario
  (seriigiAlBin+deseriigiElBin) antes de liberar el arena; o a futuro un
  mapeador Value->T generado.
- R6 [P3] kgenapi.zig: aliases de paquetes importados en *_api.zig
  (wrapper_type_name verbatim ~:1236/1309/1412/1618; hoy sin caso de uso en
  K6Bus). ~4-6 h.
- R8 [P3] Ampliar internaltests/testos: pruebas negativas del parser + GPA;
  BF_BASE64 con repeated grandes; validar packed repeated; oneof JSON con
  duplicados/last-one-wins y liberacion de rama; round-trip ZON/JSON con
  negativos; oneof texto scalar/enum.
- X1 [P1] Capa C sobre la safe API (plan, cuando se decida arrancar): emisor
  por fichero proto X_c.zig (export fns sobre X_api.zig, NUNCA el raw) + X.h
  emitido por el generador -> libX.a. Prereqs: F1 (errores) -> X2 (contrato
  FFI) -> prefijos de simbolos por fichero + .h -> tests ABI que compile el
  .h. Reintroducir root.zig/paquete solo en este escenario.
- X3 [P3] Anidados reales en el parser (backlog, asumido complejo): hoy 1
  nivel (internal_enums/internal_msgs) y message_internal_parser solo admite
  fields; las REFERENCIAS a tipos anidados no resuelven ('tipo no definido').
  Completar message_internal (enums/oneof/nested), valorar unificar
  message_parser/message_internal, resolver tipos recursivamente.
- X5 [P3] Andamiaje --ws: tests.zig con UN test por mensaje externo (hoy solo
  el primero).
- X6 [P3] Andamiaje --ws futuro: --lang zig|c|both (el workspace hoy es solo
  Zig; la variante C llegara con X1).

K6BUS (runtime, demos, seguridad)
-------------------------------------------------------------------------------
- D1 [P2] close() destructivo de un solo uso (transportes loop/udp/udpstar/
  usoxstar, generic/safe_pubsub, domain close:252-261/closeTransport:393):
  doble close o cerrar un registrado sin pasar por el Domain = UAF. Refactor
  de terminologia de cierre: close() idempotente (flag closed) + destroy()
  del dueno (era la "seccion 18" del TODO antiguo; contrato en Directrices
  5/7). Resolver ANTES de exponer runtime/pub-sub en C.
- R1 [P1] build.zig: el step 'test' (core_tests) ejecuta 0 tests -> portar
  los harness GPA de /tmp como tests del modulo (test blocks en cipher.zig
  bastan para que el step empiece a ejecutar).
- R2 [P1] cipher.zig (Aes256Gcm/ChaCha20Poly1305): pruebas de tamper/clave
  incorrecta solo en harness /tmp -> tests permanentes (round-trip, tamper de
  un byte, clave incorrecta; GPA).
- R3 [P2] Automatizar regeneracion + compilacion de ejemplos: step/script que
  regenera (protobuzig + k6b-genpubsub), compila y falla si hay diff (hoy
  manual; regeneracion byte a byte verificada).
- R4 [P2] dispatch O(canales x subscribers) -> indice canal -> lista de
  subscribers (tras medir; stream_queue.zig:154).
- R5 [P2] Gestor de claves (diseno ampliado 2026-09-08): Security.proto con
  message KeyRegistry { repeated KeyRecord }; app en 2 ficheros (CLI por
  teclado + logica; GTK APARCADA): listar con columnas marcando CADUCADAS,
  crear, borrar, enviar; almacen keys/ (un fichero por clave, propuesta);
  key_id = hash estilo hashChannel (IP/hostname + pid + contador local);
  expiracion ISO 8601 lexicografica (conectar con LoadCipher); enviar Y
  escuchar registros remotos (pub/sub de Security) con politica de merge
  pendiente de decidir. Estimaciones: fichero 2 CRUD ~4-6 h, +pub/sub/merge
  ~+3-4 h, fichero 1 ~2-3 h (+1 h recepciones en vivo).
- R7 [P3] Backlog core: Domain createFromConfigFile/destroy/flush/
  closeGraceful; CrossConnector (CreateCrossConnections :664 multi-grupo,
  reenvio Transport->Transport, evitar bucles); transportes futuros (MQTT,
  AMQP, Azure/AWS Messaging, CustomTransport+PluginLib); Logger ampliado
  (TRACE, fichero por fecha, thread id, flush, rotacion) + inconsistencia
  trace_level default 0 vs orelse 3 en domain.zig; hashes: endianness de
  hashMsgType para big-endian; benchmarks AEAD; rendimiento (benchmark
  receive 10.000 x 3 antes de decidir refcount vs direct_dispatch_to_subs);
  fachadas Vala/Go sobre la C ABI.

COMPARTIDO / PLAN
-------------------------------------------------------------------------------
- X2 [P2] Contrato FFI de memoria/ownership (decision previa a X1): regla
  unica de quien reserva/libera, strings/bytes (ptr,len) sin sentinel,
  borrowed vs owned por familia de getter.
- X4 [P3] Conops: redactar la seccion de workspace (Zig/C/both) cuando los
  conceptos operativos queden cerrados; base = DECISIONES.md (protobuzig).
  Documentar tambien: alcance de Protobuf Text/CustomTokenizer, escapes
  string/bytes, sobrescritura de defaults owned, reglas de enums, API segura
  borrowed/owned.
- Nota (decision 2026-09-09): la C ABI del CORE (exports_c.zig + k6bus.h)
  esta APARCADA a proposito; el interes actual es la capa C del GENERADO
  (safe API, X1). No es pendiente activo.

===============================================================================
2. PENDIENTES POR TIPO Y PRIORIDAD
===============================================================================

L - LEAKS (fugas de memoria)
   L1 [P2] ruta de error del parseo JSON generado filtra parcial
           (parseFromSliceLeaky; ver 1.PROTOBUZIG).

D - DOUBLE-FREE / UAF / CRASH
   D1 [P2] close() destructivo (ver 1.K6Bus) -> refactor cierre idempotente.

F - FALLOS FUNCIONALES (defectos)
   (vacia) - categoria cerrada.

R - ROBUSTEZ, TESTS Y DEUDA
   R1 [P1] step 'test' de K6Bus ejecuta 0 tests (portar harness).
   R2 [P1] cipher: tests permanentes de tamper/clave incorrecta.
   R3 [P2] automatizar regeneracion + compilacion (fallar si diff).
   R4 [P2] indice canal -> subscribers en el dispatch.
   R5 [P2] gestor de claves (KeyRegistry + CLI + logica + pub/sub + merge).
   R6 [P3] aliases de paquetes en *_api.zig (kgenapi).
   R7 [P3] backlog core (lifecycle Domain, crossconnector, transportes,
           Logger, hashes endianness, benchmarks, Vala/Go).
   R8 [P3] ampliar internaltests/testos (negativas, BASE64 grande, packed,
           oneof JSON, ZON/JSON negativos, oneof texto).

X - NUEVA FUNCIONALIDAD / PLAN
   X1 [P1] Capa C del generado sobre la safe API (prereqs F1 -> X2 ->
           prefijos/.h -> tests ABI).
   X2 [P2] Contrato FFI de memoria (decision previa a X1).
   X3 [P3] Anidados reales en el parser (2 niveles + referencias; unificar
           message_internal) - backlog, asumido complejo.
   X4 [P3] Conops + documentacion pendiente.
   X5 [P3] ws --ws: un test por mensaje externo en tests.zig.
   X6 [P3] ws --ws futuro: --lang zig|c|both.

===============================================================================
3. RESUELTOS (historial; numeracion ANTERIOR)
===============================================================================

PROTOBUZIG (generador/parser/--ws)
- 2026-09-09 RESUELTO la spec de Google del text format solo define comentarios '#'
- 2026-09-09 RESUELTO (generacion anterior, el antiguo "F2-texto"; 37decb6):
  el reader Protobuf Text generado PROPAGA errores (sin catch 0/0.0/"");
  bool con parseBoolValue case-insensitive; enum con try parseEnumValue y
  error.InvalidEnumValue. Regenerados internaltests; harness negativo 4/4.
- 2026-09-09 RESUELTO (508f704): option_parser acepta valores quoted y
  tokens con . / - etc (valoro_nuda_parser).
- 2026-09-09 Limpieza: CentralServer eliminado (obsoleto; extend no
  soportado); descriptor*.proto y sus generados fuera del repo
  (.gitignore 92727a7; se conservan locales, NO son objetivo - ver nota en
  seccion 1).
- 2026-09-09 F5 (antiguo) COMPLETO (protobuzig 0d49a58; TODO 6ca0644):
  diagnostico de service/proto3/rpc/extend; comentarios /* */; sin @panic en
  labels/tipos + validacion 'tipo no definido' + error con linea:columna;
  opciones de campo genericas [deprecated]/[jstype]/comas; sentencias
  ignorables reserved/extensions/option + hex en enums; quoted_string con
  escapes \"; other_line AVISA (decision: solo aviso, sin estricto). Fix
  colateral: centinela '\0' fuera del parse.
- 2026-09-08 R2 (antiguo): round-trip de repeated via Protobuf Text en
  testo3 (repeated message + string, >=2 elementos, GPA).
- 2026-09-08 Andamiaje --ws: workspace autocontenido; opcion A (sin root.zig
  ni modulo raiz en solo-Zig); main con muestra automatica;
  examples/prueba1.
- 2026-09-08 DECISIONES.md: registro de diseno base del conops.
- 2026-09-08 Renombrado example -> internaltests + tareas VSCode corregidas.
- 2026-09-08 F1 (antiguo): escritor Protobuf Text escapa strings/bytes.
  Regenerados K6Bus 666e8ed/9e57108 (incluye fix seriigiAlDosiero).
- 2026-09-07 F4 (antiguo): codigo muerto + propagacion de errores de
  escritura en la emision.
- 2026-09-07 F3 (antiguo): defaults de declaracion + plenigiDefaultojn.
- 2026-09-07 F2 (antiguo): deserializacion binaria con try + errdefers.
- 2026-09-07 L2: ArenaAllocator de generacion. F6: logger portable. L1:
  initDefault con errdefers (09-05/07).
- Antes: CustomTokenizer (comillas escapadas, numeros, '#'); oneof Protobuf
  Text (message); fusion Msg+Packet en types.proto; repeated Protobuf Text;
  enums y reglas validadas; API segura (kgenapi) completa.

K6BUS (runtime/demos/seguridad)
- 2026-09-08 TODO reestructurado (0f4e5f7); R6 (antiguo) disenado/agendado;
  F1 regenerados (666e8ed); fix seriigiAlDosiero (9e57108).
- 2026-09-07 L3: slices prestados de AppConfig en los 4 transportes (EndPoint
  .host OWNED); Security.proto KeyRecord con created_on/expires_on; Logger
  campo *Logger por struct (fin de globals).
- 2026-09-04 D2 + C ABI prototipo (exports_c/k6bus.h manual); fusion
  types.proto; demos regeneradas; config VSCode demo1.
- 2026-08-31 demos a la API segura; k6b-genpubsub dual; filtrado msgType como
  contrato; QueueMgr.stop() sin hang; plantilla de ciclo de vida en los 4
  transportes; processMsgList con defer freeMsgsFromSlice.
- 2026-08-30/31 cifrado real: AES-256-GCM y ChaCha20-Poly1305; CBC eliminado;
  validacion de 32 bytes en create().
- Antes: enqueueMany ownership unico; packet_processor defer
  freeClonedMsgSlice; doble errdefer udpstar/usoxstar; cipher.deinit en
  Domain + errdefers LoadCipher; GetDomainCfg -> createDomain.

-------------------------------------------------------------------------------
=== NUMERACION ACTUAL (2026-09-09 en adelante): los IDs de aqui ya son de la
generacion nueva (D1, F1..., R1..R8, X1..X6). Las entradas de hoy sin ID
(parseo silencioso, option quoted, limpieza CentralServer/descriptors) son
tambien de esta generacion. ===
-------------------------------------------------------------------------------
- 2026-09-09 F1 (tokenizer '//') CERRADO por decision: la spec oficial de
  Google del text format solo define comentarios '#' (hasta fin de linea);
  '//' y '/* */' son del lenguaje .proto (esquema), no del text format. El
  CustomTokenizer generado ya cumple con '#': no se implementa '//' como
  extension no estandar.

NOTA: este documento sustituye a las antiguas secciones 1-19. La numeracion
de la generacion actual (D1, F1..., R1..R8, X1..X6) es independiente de los
IDs historicos citados arriba (numeracion anterior). Politica: no renumerar
salvo en lotes grandes de resueltos. Detalle historico fino: git y commits.
