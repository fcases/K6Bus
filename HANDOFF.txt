K6BUS / PROTOBUZIG - HANDOFF
Actualizado: 2026-09-04

PROPOSITO DE ESTE DOCUMENTO
Este fichero contiene el checkpoint exacto para continuar el desarrollo. Debe
reemplazarse en cada relevo y no acumular la historia completa del proyecto.

Estilo de trabajo:
- espanol tecnico informal;
- trabajo paso a paso;
- revision critica de arquitectura;
- codigo concreto;
- sencillez y ownership explicito;
- humor tecnico y algun guiño en esperanto.

Fernando es kunulo/kamarado. La IA es kun-mashino.

===============================================================================
1. CHECKPOINT ACTUAL
===============================================================================

La campana de leaks/double-free del nucleo K6Bus esta PRACTICAMENTE CERRADA
(inventario en seccion 2). El frente inmediato pasa a: guard defensivo de
idempotencia en close(), limpieza de demo1, items del generador ProtobuZig y
documentacion del esquema de protos.

Hechos recientes consolidados (2026-09-04):

- FUSION Msg+Packet en protos/types.proto (package k6bus), commiteada y
  pusheada. Elimina la UNICA importacion cruzada de protos de K6Bus y con ella
  el gap de kgenapi para el core (types_api.zig sin referencias punteadas,
  compila). Regenerados types.zig/types_api.zig con el toolchain de Desktop:
  BYTE A BYTE identicos a la salida de protobuzig_v2 (toolchains en sync).
  Borrados Msg.zig/Packet.zig y sus *_api.zig. Imports del core migrados a
  k6bus.Msg / k6bus.Packet (12 ficheros + root.zig + build.zig con un solo
  paso gen_types). Wire SIN cambios (mismos field numbers). check_all verde.
- processMsgList (item B): fix aplicado - defer Utils.freeMsgsFromSlice al
  inicio cubre TODAS las salidas (initDefault/cloneMsgSlice/seriigiAlBin con
  catch return ya no filtran el msg_list original); free manual eliminado;
  stats.free_ns medido dentro del defer. Espejo de dispatchToTransports.
  Verificado: zig build + zig fmt OK.
- root.zig:47 corregido: UDPStarEndPoint = EndPoint real de udpstar_transport.
- DESCUBRIMIENTO: 04_protobuzig/K6Bus es un SYMLINK a 06_k6bus/K6Bus - es UN
  solo arbol fisico. Obsoleta la antigua advertencia de "dos checkouts".
- Herramientas: tools/protobuzig operativo (symlink ->
  /home/chuco/Desktop/protobuzig_v2/zig-out/bin/protobuzig, compilado; Desktop
  tambien tiene testo3).

Hitos anteriores (resumen, detalle en TODO §16):
- msgType filter como contrato de registro (2026-08-31).
- Plantilla de ciclo de vida en los 4 transportes; QueueMgr.stop() con 2º
  broadcast (2 stop() concurrentes sin hang).
- Demo1 y demo2 migradas a la API segura con cierre via closeSubscriber().
- Cifrado real: AES-256-GCM y ChaCha20-Poly1305 (framing nonce|ct|tag); CBC
  ELIMINADO (Security.proto con reserved; cipher.zig sin rama CBC;
  CRYPTO_CUSTOM reservado).
- Generados regenerados con el generador actual (CustomTokenizer,
  unescapePbTextToken sin catch "", parsed_owned, deinitParams, etc.).

===============================================================================
2. TRABAJO EN CURSO
===============================================================================

Inventario de leaks/double-free, estado a 2026-09-04:

DOUBLE-FREE / LEAKS DEL NUCLEO: CERRADOS
- enqueueMany contrato de ownership unico.                          [OK]
- Doble errdefer udpstar/usoxstar -> errdefers paulatinos.          [OK]
- close() transportes por contrato Domain.                          [OK]
- Domain.deinit cipher + errdefers LoadCipher.                      [OK]
- packet_processor rutas RX (freeClonedMsgSlice) y TX processMsgList[OK]
- GetDomainCfg -> createDomain (dominio default en app_cfg.domains, [OK]
  borrowed durante init; sin leak del branch default).
- estacion_pubsub.zig (demo1): muerto (demo1 migrado); borrarlo es   [limpieza]
  la unica accion pendiente.
- catch "" generados / parsed_owned / deinitParams / Estacion.zig    [OK]
- QueueMgr.stop() 2º broadcast.                                      [OK]
- Referencia rota root.zig:47 (EndPoint).                            [OK]

PENDIENTES ABIERTOS
- Guard defensivo de idempotencia en close() de transportes (doble
  close directo de la app = double-free; el contrato del Domain lo evita,
  pero un guard closed seria mas robusto).                            [PENDIENTE]
- exports_c.zig:21 (importa Hash inexistente): DIFERIDO a proposito -
  exports_c es el prototipo experimental de la futura C ABI (se disenara de
  cabo a rabo tras la v1 Zig); nada lo analiza hoy.                  [DIFERIDO]
- Generador ProtobuZig (protobuzig_v2): initDefault sin errdefer,
  page_allocator disperso, ESCRITOR Protobuf Text sin escapes
  (comillas/barra inversa - FALLA verificada), decodeVarint catch 0
  (parse silencioso), helpers legacy con catch "".                  [PENDIENTE]
- cfg raiz k6bus.App.zon.cfg sin .cross_connectors = .{} (o defaults
  de declaracion en el generador).                                   [PENDIENTE]
- Pruebas permanentes de manipulacion/claves incorrectas (hoy harness
  en /tmp); app generadora de claves (Security.proto/KeyRecord).     [PENDIENTE]

NOTAS DE DISENO (2026-09-04)
- Gap kgenapi: emite tipos de campos message verbatim del .proto
  ("k6bus.msg.Msg" + "Impl") sin emitir el alias del paquete importado.
  Hoy SIN caso de uso en K6Bus (types.proto no importa nada). Si algun
  proto vuelve a importar otro, arreglar kgenapi (emitir bloque alias del
  paquete importado: wrapper desde <File>_api.zig + Impl desde el raw).
- Incluso el generador RAW resuelve imports por heuristica fragil
  (kgen_auks: ultimo segmento del tipo == alias del modulo importado;
  exige nombre de fichero == nombre de mensaje). Evitar imports cruzados
  mientras se pueda.

===============================================================================
3. SIGUIENTE PASO EXACTO
===============================================================================

1. Cerrar el guard defensivo de idempotencia en close() de los 4 transportes
   (patron: flag closed + return temprano; verificar doble close con GPA).
2. Limpieza demo1: borrar estacion_pubsub.zig y root.zig de examples/demo1
   (ya no se compilan; su borrado elimina el leak del item 7).
3. Actualizar el cfg raiz k6bus.App.zon.cfg (anadir .cross_connectors = .{}
   o resolver via defaults de declaracion en el generador - TODO §7).
4. Generador ProtobuZig (protobuzig_v2): primero el ESCRITOR Protobuf Text
   con escapes (TODO §3, primer item de esa seccion).
5. Test de repeated Protobuf Text en testo3 (TODO §1.2).
6. Automatizar tests del runtime K6Bus (hoy el step "test" ejecuta 0 tests).
7. Redisenar el filtrado como contrato (canal, tipos aceptados) si procede
   tras medir; benchmark N x S antes de decidir refcount/decode-once.
8. Cifrado: pruebas permanentes de tamper/claves; app generadora de claves.

No saltar a refcount ni a la C ABI definitiva hasta cerrar 1-4.

===============================================================================
4. CRITERIOS DE ACEPTACION DEL MINIPASO
===============================================================================

- Ningun cierre (close) produce double-free ni leaks, aunque haya mensajes en
  vuelo durante el shutdown.
- Ninguna ruta de error de create/init produce double-free ni leaks.
- GPA reporta cero leaks en las pruebas consolidadas de los demos.
- El codigo generado no contiene catch "" ni catch 0 tras regenerar.
- Los transportes pueden cerrarse dos veces sin fallar (close idempotente).
- check_all verde tras cada cambio.

===============================================================================
5. REGLAS QUE NO SE DEBEN ROMPER
===============================================================================

- initDefault()/deinit() es la pareja estandar.
- Los mensajes owned no se copian superficialmente.
- Antes de sobrescribir memoria owned se libera el valor anterior.
- get de string/bytes es borrowed.
- get de message devuelve copia owned.
- oneof libera la rama previa antes de activar otra.
- Repeated preserva orden.
- ZON/JSON no crean defaults owned implicitos tras parsear.
- La configuracion AppConfig es temporal durante init.
- El dispatch_fn de QueueMgr consume los internals de msg_list en TODAS las
  salidas (defer al inicio); el ARRAY exterior pertenece a QueueMgr.mainLoop
  y nunca se libera en el dispatch_fn (quien asigna, libera).
- El wire no transporta metadatos de runtime: si algun dia se comparte payload
  (refcount), vivira como cabecera de asignacion, nunca como campo de Msg o
  Packet.

===============================================================================
6. TRAMPAS PRACTICAS
===============================================================================

- Recompilar ProtobuZig despues de modificar el generador.
- K6Bus es UN solo arbol: 04_protobuzig/K6Bus es symlink de 06_k6bus/K6Bus.
  No duplicar cambios "en el otro checkout": no existe.
- Los protos core viven en K6Bus/protos (types.proto, Config.proto,
  Security.proto); NO crear imports cruzados entre ellos mientras kgenapi no
  emita aliases de paquetes importados.
- Regenerar los generados de K6Bus con tools/protobuzig (Desktop toolchain)
  y verificar byte a byte contra protobuzig_v2 si se duda de la sync.
- Las pruebas de leaks se montan en /tmp en un SOLO comando (el /tmp del
  sandbox es efimero entre comandos) con ZIG_GLOBAL_CACHE_DIR propio.
- check_all bajo el sandbox falla por ReadOnlyFileSystem en las demos
  (escriben .zig-cache en la ruta real); ejecutarlo en el entorno local.
- Mantener las pruebas permanentes en testo3 cuando ejerciten la API publica.
- Los ficheros token.txt (protobuzig_v2) y zig-pa1.token.txt (~/MyProgs/ZIG)
  contienen tokens antiguos ya revocados: no reintroducir tokens en el arbol.

===============================================================================
7. DESPUES DEL MINIPASO
===============================================================================

Orden corto recomendado:
1. Cerrar los pendientes de la seccion 2 (guard idempotencia, limpieza demo1,
   cfg raiz, items del generador).
2. Anadir el test de repeated Protobuf Text en testo3.
3. Automatizar tests del runtime K6Bus.
4. Benchmark del escenario N mensajes x S subscribers antes de decidir
   refcount/decode-once.
5. Despues de estabilizar la v1 Zig: API C completa de cabo a rabo (otro
   "todo"), luego Vala (vapi) y Go (cgo).

===============================================================================
8. DOCUMENTOS DE REFERENCIA
===============================================================================

- Resumen.txt: vision y estado general.
- Directrices.txt: decisiones y reglas estables.
- CONOPS.txt: funcionamiento operativo previsto.
- TODO.txt: backlog completo y criterios de aceptacion.
- HANDOFF.txt: este checkpoint concreto.

Bonan daurigon, kunulo.
