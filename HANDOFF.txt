## HANDOFF.txt
## ============================================================================
## K6Bus / ProtobuZig - Handoff para nueva conversacion
## ============================================================================
Fecha de actualizacion: 2026-08-18
Estilo de trabajo: espanol tecnico informal, paso a paso, revision critica, codigo concreto, humor tecnico y guiños en esperanto.
Nota de trato: Fernando es kunulo/kamarado. La IA es kun-mashino.

## ============================================================================
## 1. Objetivo del handoff
## ============================================================================
Este fichero permite arrancar una nueva conversacion sin depender del historial completo del chat anterior.

La conversacion siguiente debe continuar el trabajo en K6Bus y ProtobuZig manteniendo estas reglas:
- Diseno sencillo antes que abstraccion elegante.
- Ownership explicito.
- initDefault()/deinit() como pareja estandar en codigo generado.
- La logica real vive en Zig.
- APIs C, Vala, Go u otros lenguajes son fachadas sobre Zig.
- mecha no es parte del proyecto; es una libreria externa usada por ProtobuZig para parsing.
- Trabajar paso a paso, con confirmacion antes de cada bloque grande.

## ============================================================================
## 2. Estado estable alcanzado
## ============================================================================
Se ha alcanzado un checkpoint estable con avances relevantes en ProtobuZig y K6Bus.

Estado validado historico:
- Config.proto usa oneof params en TransportConfig.
- ProtobuZig parsea oneof y lo representa en AST mediante OneOfDecl / OneOfField.
- ProtobuZig genera Zig para oneof como union(enum) con none: void.
- initDefault() inicializa oneof a none.
- deinit() libera la rama activa del oneof.
- seriigi() binario serializa la alternativa activa del oneof.
- deseriigi() binario reconoce alternativas oneof por field_number, libera la alternativa previa y asigna la nueva.
- K6Bus lee k6bus.App.zon.cfg con TransportConfig.params como union(enum).
- La sintaxis ZON correcta usa .{ ... } y no &.{ ... }.
- Domain.create/createEx cargan la configuracion, usan AppConfig/DomainConfig durante init y liberan AppConfig despues.
- Domain ya no conserva el DomainConfig completo parseado; conserva solo configuracion runtime minima.
- Demo1 ejecuta y cierra sin leaks segun GPA.

Estado validado reciente:
- kgenapi.zig genera wrappers *_api.zig.
- kgapi_auks.zig contiene helpers de API segura.
- La API segura genera initDefault/deinit/clone/IO comun.
- Se generan accessors para required/optional/repeated scalar/enum.
- Se generan accessors para required/optional/repeated string/bytes.
- Se generan accessors para required/optional/repeated message.
- Se generan accessors completos para oneof message/scalar/enum/string/bytes.
- cloneImpl(comptime T) usa seriigiAlBin + deseriigiElBin.
- Round-trip binario preserva orden en repeated scalar, repeated string y repeated message.
- BF_BASE64 libera el buffer temporal decoded tras deserializar.
- Optional con default se serializa si != null aunque el valor sea igual al default.
- Testo3 valida EstMeteo, SnrTrafico, UnixSocketStarConfig, AppConfig, clone, repeated message y oneof.

## ============================================================================
## 3. Arquitectura actual de K6Bus
## ============================================================================
K6Bus es un middleware Pub/Sub en Zig orientado a sistemas, robotica, automatizacion, SCADA y aplicaciones de tiempo real.

Piezas principales:
- Domain es el nucleo local del bus.
- No hay singleton global.
- Puede haber multiples Domain en un mismo proceso.
- Domain se crea en heap y se referencia mediante puntero estable.
- Publishers publican mediante Domain.
- Subscribers reciben de forma desacoplada mediante cola e hilo propios.
- StreamQueueDOWN distribuye hacia transportes y entrega local.
- StreamQueueUP distribuye hacia subscribers.
- QueueMgr gestiona colas, threads, condiciones, batching y dispatch.
- Los transportes concretos son objetos reales que mueven bytes.
- PacketProcessor no debe ser confundido con transporte.

## ============================================================================
## 4. Configuracion y ownership
## ============================================================================
Decision cerrada:
- AppConfig y DomainConfig completos son configuracion temporal de arranque.
- Domain no conserva el arbol completo de configuracion tras init.
- Domain conserva solo una configuracion runtime pequena con valores baratos:
  - binary_format
  - dispatch_mode
  - dispatch_batch_time_ms
  - start_at_init
- LoadTransports(dom_cfg) y CreateCrossConnections(dom_cfg) reciben DomainConfig temporal durante init.
- app_cfg.deinit(allocator) se ejecuta despues de init, liberando memoria parseada desde ZON.

Condicion importante:
- Todo transporte o componente creado desde configuracion debe copiar cualquier string/slice que vaya a conservar despues de createEx().
- Si solo usa strings durante creacion, no necesita copiarlos.

## ============================================================================
## 5. Estado de API segura generada
## ============================================================================
La API segura generada vive en ficheros *_api.zig.

Forma general:
- pub const Tipo = struct { impl: TipoImpl }.

Funciones comunes:
- initDefault()
- deinit()
- clone()
- writeToText()/readFromText()
- serializeToBin()/deserializeFromBin()
- writeToFile()/readFromFile()

Accessors generados:
- required scalar/enum: setX/getX.
- optional scalar/enum: setX/getX/hasX/clearX.
- repeated scalar/enum: appendX/setX/clearX/getXCount/getXAt.
- required string/bytes: setX/getX.
- optional string/bytes: setX/getX/hasX/clearX.
- repeated string/bytes: appendX/setX/clearX/getXCount/getXAt.
- required message: setX/getX con copia profunda.
- optional message: setX/getX/hasX/clearX con copia profunda.
- repeated message: appendX/clearX/getXCount/getXAt con copia profunda.
- oneof: has general, clear general, set/has/get por rama.

Reglas:
- get de message devuelve copia owned y requiere deinit().
- get de string/bytes devuelve vista borrowed.
- set/append de message clona profundamente.
- oneof set clona/duplica primero, luego libera rama anterior.

## ============================================================================
## 6. Estado de oneof
## ============================================================================
Ya hecho:
- Parser reconoce oneof.
- AST ampliado con OneOfDecl y OneOfField.
- Resolucion de tipos no resueltos aplicada tambien a campos oneof.
- Impresion por consola muestra oneofs.
- Generacion Zig de union(enum), incluyendo alternativa none: void.
- Generacion del campo oneof en la struct.
- initDefault() inicializa oneof a none.
- deinit libera la alternativa activa.
- seriigi/deseriigi binarios implementados para alternativas oneof.
- ZON de entrada validado con union(enum).
- Demo1 sin leaks tras lectura ZON y cierre.
- API segura cubre oneof message/scalar/enum/string/bytes.
- Test de PanelBase.datos valida set/has/get/clear, cambio de rama y round-trip binario.

Pendiente:
- oneof en Protobuf Text raw.
- Validacion/generacion especifica de oneof en JSON si hace falta.
- Validacion de escritura/lectura ZON round-trip con union(enum).
- oneof dentro de mensajes internos mas adelante.

## ============================================================================
## 7. BinaraFormato vs BinaryFormat
## ============================================================================
Estado actual:
- ProtobuZig genera BinaraFormato.
- Config.proto define BinaryFormat.
- Ambos enums tienen valores alineados en la parte comun.

BinaraFormato actual esperado:
- BF_PROTOBUF = 0
- BF_OMG_CDR = 1
- BF_ASN1_BER = 2
- BF_ASN1_DER = 3
- BF_BASE64 = 10
- BF_BINPB2TEKSTO_HEX = 11
- BF_BINPB2TEKSTO_DEC = 12

BinaryFormat en Config.proto:
- BF_PROTOBUF = 0
- BF_OMG_CDR = 1
- BF_ASN1_BER = 2
- BF_ASN1_DER = 3

Nota:
- Aunque ambos sean enum(u64), Zig los trata como tipos distintos.
- Se recomienda conversion explicita por valor:

pub fn binaryFormatToBinaraFormato(v: BinaryFormat) BinaraFormato {
    return std.meta.intToEnum(BinaraFormato, @intFromEnum(v)) catch .BF_PROTOBUF;
}

## ============================================================================
## 8. Deudas tecnicas importantes
## ============================================================================
Pendiente relevante en ProtobuZig:
- Revisar generacion de defaults para string/bytes.
- Evitar defaults literales en campos owned que deinit() libera.
- Centralizar defaults string/bytes owned en initDefault().
- Implementar oneof en Protobuf Text/JSON/ZON round-trip.
- Revisar packed repeated tras el cambio de orden.

Pendiente en generador:
- Usar ArenaAllocator para memoria temporal de generacion, en vez de page_allocator disperso.
- Revisar memory ownership de strings generados por allocPrint/concatWithSentinel.

Pendiente en parser:
- message_internal_parser debe soportar fields, enums internos, mensajes internos y oneofs.
- Impresion recursiva por consola de mensajes internos.

## ============================================================================
## 9. Tareas recomendadas al retomar
## ============================================================================
Orden recomendado:
1. Hacer commit del bloque message + oneof si no se ha hecho.
2. Regenerar K6Bus con el binario correcto de ProtobuZig.
3. Revisar que transportes no guarden slices prestados desde AppConfig liberado.
4. Anadir helper BinaryFormat -> BinaraFormato por valor donde corresponda.
5. Implementar oneof en Protobuf Text raw.
6. Probar escritura/lectura ZON con union(enum).
7. Revisar JSON para oneof.
8. Abordar defaults string/bytes owned.
9. Completar message_internal_parser recursivo.
10. Pensar API C manual de K6Bus runtime antes de generar C ABI para mensajes.

## ============================================================================
## 10. Arranque recomendado del siguiente chat
## ============================================================================
Mensaje sugerido:
"Estoy trabajando en K6Bus/ProtobuZig. Lee HANDOFF, CONOPS, Resumen, Directrices y TODO. Ya hay API segura generada *_api.zig con accessors completos para scalar/string/message/oneof, cloneImpl por binario y tests en testo3 sin leaks. Siguiente frente: commit/regeneracion K6Bus, revisar strings prestados en transportes, BinaryFormat/BinaraFormato y oneof en Protobuf Text."
