syntax = "proto2";
package k6bus;
option optimize_for = SPEED;

message Msg {
    repeated fixed64 channels = 1;
    required fixed64 msgType = 2;
    required bytes payLoad = 3;
}

message Packet {
    repeated Msg messages = 1;
    optional uint64 OutOfBand = 2;
}
